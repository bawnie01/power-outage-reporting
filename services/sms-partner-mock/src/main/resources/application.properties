spring.application.name=sms-partner-mock
server.port=8081
sms.mock.timeout-delay-ms=${SMS_MOCK_TIMEOUT_DELAY_MS:5000}
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
